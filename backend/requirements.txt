fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.39
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
python-dotenv==1.0.1
bleach==6.3.0
psycopg2-binary==2.9.10
httpx==0.28.1
pywebpush==2.3.0
py-vapid==1.9.4
pytest==8.3.4
slowapi==0.1.9
# Python's zoneinfo reads the SYSTEM tz database, which Windows does not have.
# Without this, ZoneInfo("Africa/Lagos") raises on any Windows dev machine and
# User.local_today() silently falls back to UTC -- i.e. the timezone-aware
# streak logic appears to do nothing locally while working fine on Render's
# Linux hosts. Harmless (unused) on Linux, essential on Windows.
tzdata==2025.2
# Reading school-uploaded question spreadsheets (app/exam_import.py).
openpyxl==3.1.5
